# Mission 007.5 — MCP 2 Security Compatibility Interlock

Created: 2026-08-29 14:23 CT  
Status: **OPEN — READY TO DISPATCH**  
Route: **Codex**  
TD: **ROUTE✓**

## Why this mission exists

Mission 007 is complete, but the dependency-security remediation performed afterward exposed one blocking compatibility break before Mission 008 should begin.

Observed local remediation state on 2026-08-29:

- frontend `npm audit`: **0 vulnerabilities**;
- frontend production build: **PASS, 432 modules transformed**;
- Python `pip-audit`: **No known vulnerabilities found** after the security upgrades;
- Starlette upgraded from **0.50.0 to 1.6.0**;
- MCP resolved to **2.1.1**;
- backend test collection then stopped with **6 collection errors** because inherited MCP code still imports the MCP 1.x API `mcp.server.fastmcp.FastMCP`;
- MCP 2.x reports that `FastMCP` was renamed to `MCPServer` and other APIs changed.

Remote `main` was independently observed at mission creation as:

`1dcca51c3c20d0c4dde3d14892de512f5ebf7f17` — `docs: update ModelMix coding agent instructions`

The dependency-security changes above are local/uncommitted state and are **not claimed to exist on remote `main` yet**. Verify the working tree before editing. If the expected local dependency changes are missing, recover/reconcile them rather than blindly recreating prior work.

## Objective

Migrate the existing inherited MCP integration to the current MCP 2.x API so ModelMix can keep the security-clean dependency set without carrying an avoidable MCP 1.x compatibility pin.

This is a compatibility/security interlock only. It does **not** promote MCP into ModelMix alpha product scope.

## Required behavior

- Replace MCP 1.x-only server usage with the MCP 2.x-supported API and update only directly affected integration/tests.
- Preserve existing MCP tool/server behavior unless MCP 2.x requires a narrowly documented compatibility change.
- Keep MCP 2.x; do not solve the mission by pinning `mcp<2` unless a verified blocker makes migration impossible and that blocker is reported before fallback.
- Preserve the security dependency upgrades already made unless a specific incompatibility is demonstrated.
- Do not redesign ModelMix orchestration, worker independence, Moderator flow, persistence, UI, or provider architecture.
- Do not expand MCP into an alpha feature; this work only keeps inherited repository code compatible and testable.

## Verification

1. MCP-specific test modules collect and run successfully.
2. Run the full backend test suite.
3. If full-suite failures remain, determine whether they are caused by this dependency/security work. Fix in-scope regressions; do not silently absorb unrelated pre-existing failures.
4. Re-run the Python dependency audit and require **0 known vulnerabilities** for the locked dependency set before acceptance.
5. Re-run frontend `npm audit` and production build only if dependency state changed after the last observed clean frontend verification; otherwise preserve the existing observed result rather than burning cycles.
6. Inspect the final diff for unrelated changes and credential leakage.
7. Commit and push only after verification is acceptable, then verify the resulting commit exists on remote `main`.

## Deliverable

A focused MCP 2.x compatibility/security commit plus an updated Mission 007.5 record containing:

- exact files changed;
- exact MCP version retained;
- audit results;
- tests actually run and observed results;
- any remaining unrelated failures, if present;
- commit SHA;
- remote verification.

Mission 008 remains the next planned product mission after this interlock closes.